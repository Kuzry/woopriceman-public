import { createStyles } from "@mantine/core";

const PaperHeader_useStyles = createStyles((theme, _params, getRef) => ({
  PaperContent: {
    padding: "20px",
  },
}));

export default PaperHeader_useStyles;
